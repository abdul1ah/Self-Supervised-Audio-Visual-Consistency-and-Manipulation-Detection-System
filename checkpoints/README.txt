Place your trained checkpoint here as best_model.pth (see src/train.py / src/evaluate.py),
or set the environment variable CHECKPOINT_PATH to another file.

The FastAPI backend loads weights from checkpoints/best_model.pth by default.
